# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**RaboTA** - Внутренний сайт для управления рабочими процессами и инструментов для личной продуктивности.

Проект представляет собой модульное веб-приложение с различными функциональными блоками. Каждый модуль доступен через главную страницу и работает как отдельное приложение внутри единой экосистемы.

### Текущие модули

- **График работы** (`/schedule`) - Система управления рабочими сменами и сотрудниками
  - Управление сотрудниками
  - Создание и редактирование смен
  - Визуальный календарь с назначением смен
  - Хранение данных в PostgreSQL через REST API

## Technology Stack

### Frontend
- **React 19.2.0** - UI библиотека
- **TypeScript 5.9.3** - Типизация
- **Vite 7.2.2** - Сборщик и dev сервер
- **React Router 7.9.6** - Маршрутизация

### Backend
- **Node.js 20.x** - JavaScript runtime
- **Express 5.x** - Web framework
- **TypeScript 5.9.3** - Типизация
- **PostgreSQL 15** - Реляционная база данных
- **pg 8.16.3** - PostgreSQL клиент для Node.js

### Styling
- **Tailwind CSS 3.4.18** - Utility-first CSS фреймворк
- **PostCSS 8.5.6** - Обработка CSS
- **Autoprefixer 10.4.22** - Автоматические вендорные префиксы

### UI Components
- **lucide-react 0.553.0** - Иконки
- **react-colorful 5.6.1** - Выбор цвета

### Development Tools
- **ESLint 9.39.1** - Линтер
- **TypeScript ESLint 8.46.3** - TypeScript правила для ESLint
- **Nodemon 3.1.11** - Auto-reload для разработки
- **Docker & Docker Compose** - Контейнеризация

### Deployment
- **Nginx** - Веб-сервер для production
- **PM2** - Process manager (альтернатива Docker)

## Project Structure

```
RaboTA/
├── backend/                    # Backend API сервер
│   ├── src/
│   │   ├── config/            # Конфигурация (database.ts)
│   │   ├── controllers/       # Контроллеры API
│   │   │   ├── employeeController.ts
│   │   │   ├── shiftController.ts
│   │   │   └── scheduleController.ts
│   │   ├── db/               # База данных
│   │   │   ├── schema.sql    # SQL схема
│   │   │   └── migrations.ts # Миграции
│   │   ├── models/           # Модели данных
│   │   │   └── types.ts      # TypeScript типы
│   │   ├── routes/           # API маршруты
│   │   │   ├── employeeRoutes.ts
│   │   │   ├── shiftRoutes.ts
│   │   │   └── scheduleRoutes.ts
│   │   └── server.ts         # Точка входа сервера
│   ├── package.json          # Зависимости backend
│   ├── tsconfig.json         # TypeScript конфигурация
│   ├── .env.example          # Пример переменных окружения
│   └── Dockerfile            # Docker образ для backend
├── work-schedule-app/        # Frontend приложение
│   ├── src/
│   │   ├── components/       # Переиспользуемые компоненты
│   │   │   ├── EmployeeManager.tsx
│   │   │   ├── ScheduleCalendar.tsx
│   │   │   └── ShiftManager.tsx
│   │   ├── pages/            # Страницы приложения
│   │   │   ├── Home.tsx      # Главная страница (/)
│   │   │   └── Schedule.tsx  # График работы (/schedule)
│   │   ├── hooks/            # Custom React hooks
│   │   │   └── useLocalStorage.ts
│   │   ├── services/         # API сервисы
│   │   │   └── api.ts        # REST API клиент
│   │   ├── types.ts          # TypeScript типы
│   │   ├── App.tsx           # Главный компонент с роутингом
│   │   ├── main.tsx          # Точка входа
│   │   ├── index.css         # Глобальные стили
│   │   └── App.css           # Стили компонента App
│   ├── public/               # Статические файлы
│   ├── index.html            # HTML шаблон
│   ├── package.json          # Зависимости frontend
│   ├── tsconfig.json         # TypeScript конфигурация
│   ├── vite.config.ts        # Vite конфигурация
│   ├── tailwind.config.js    # Tailwind конфигурация
│   ├── postcss.config.js     # PostCSS конфигурация
│   ├── eslint.config.js      # ESLint конфигурация
│   ├── .env.example          # Пример переменных окружения
│   ├── Dockerfile            # Docker образ для frontend
│   └── nginx.conf            # Nginx конфигурация
├── docker-compose.yml        # Docker Compose конфигурация
├── DEPLOYMENT.md             # Руководство по развертыванию
├── CLAUDE.md                 # Этот файл
└── .gitignore                # Git ignore правила
```

## Architecture

### Routing
Приложение использует React Router для навигации:
- `/` - Главная страница с карточками модулей
- `/schedule` - Модуль управления графиком работы

### Data Management
- **PostgreSQL Database** - Серверное хранилище данных
- **REST API** - Backend API для взаимодействия с БД
- **State Management** - Используется встроенный React state (useState)
- **Legacy**: Local Storage (в процессе миграции на API)

### Database Schema
- **employees** - Сотрудники (id, name, exclude_from_hours)
- **shifts** - Смены (id, name, abbreviation, color, hours, is_default)
- **schedule** - График (id, employee_id, day, month, year, shift_id)

### Component Structure
- **Pages** - Страницы верхнего уровня с роутингом
- **Components** - Переиспользуемые компоненты для конкретных функций
- **Hooks** - Custom React hooks для общей логики
- **Services** - API клиенты для взаимодействия с backend

## Development Commands

### Frontend (work-schedule-app/)

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (http://localhost:5173)
npm run dev

# Сборка для production
npm run build

# Предварительный просмотр production сборки
npm run preview

# Запуск линтера
npm run lint
```

### Backend (backend/)

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (http://localhost:3001)
npm run dev

# Сборка для production
npm run build

# Запуск production сервера
npm start

# Запуск миграций БД
npm run migrate
```

### Docker Compose (root/)

```bash
# Запуск всех сервисов
docker compose up -d

# Остановка всех сервисов
docker compose stop

# Просмотр логов
docker compose logs -f

# Пересборка и запуск
docker compose up --build -d
```

## Git Workflow

Проект использует Git для контроля версий:
- **Repository**: git@github.com:xyzmean/rabota_dev.git
- **Branch**: master

### Commit Guidelines
- Используйте осмысленные сообщения коммитов
- Коммиты создаются через Claude Code с автоматическим footer

## Development Guidelines

### Adding New Modules
1. Создайте новую страницу в `src/pages/`
2. Добавьте маршрут в `src/App.tsx`
3. Добавьте карточку модуля на главной странице `src/pages/Home.tsx`
4. Создайте необходимые компоненты в `src/components/`
5. Создайте API endpoints в backend если требуется

### Styling
- Используйте Tailwind CSS классы для стилизации
- Следуйте существующей цветовой схеме (синий градиент для header)
- Адаптивный дизайн через Tailwind responsive breakpoints

### TypeScript
- Все типы должны быть определены в `src/types.ts` или локально в компонентах
- Избегайте использования `any`
- Используйте строгую типизацию

## Server Specifications

Проект оптимизирован для работы на сервере со следующими характеристиками:
- **CPU**: 1 ядро
- **RAM**: 2GB
- **Storage**: 30GB
- **Users**: 3-4 одновременных пользователя

### Database Optimization
- PostgreSQL настроен с ограниченными ресурсами:
  - `shared_buffers = 128MB`
  - `max_connections = 20`
  - `work_mem = 8MB`
- Индексы для быстрого поиска по датам и сотрудникам

## API Endpoints

### Employees
- `GET /api/employees` - Получить всех сотрудников
- `GET /api/employees/:id` - Получить сотрудника по ID
- `POST /api/employees` - Создать сотрудника
- `PUT /api/employees/:id` - Обновить сотрудника
- `DELETE /api/employees/:id` - Удалить сотрудника

### Shifts
- `GET /api/shifts` - Получить все смены
- `GET /api/shifts/:id` - Получить смену по ID
- `POST /api/shifts` - Создать смену
- `PUT /api/shifts/:id` - Обновить смену
- `DELETE /api/shifts/:id` - Удалить смену

### Schedule
- `GET /api/schedule?month=X&year=Y` - Получить график (с фильтрацией)
- `GET /api/schedule/:id` - Получить запись по ID
- `POST /api/schedule` - Создать запись
- `PUT /api/schedule/:id` - Обновить запись
- `DELETE /api/schedule/:id` - Удалить запись
- `POST /api/schedule/bulk` - Массовое создание/обновление
- `POST /api/schedule/delete-by-date` - Удалить по дате и сотруднику

### Health
- `GET /health` - Проверка состояния сервера

## Migration Status

**Текущий статус**: Backend реализован, frontend частично интегрирован

### Выполнено:
- ✅ Backend API сервер (Express + TypeScript)
- ✅ PostgreSQL схема и миграции
- ✅ REST API endpoints для всех сущностей
- ✅ Docker Compose конфигурация
- ✅ API сервис слой для frontend
- ✅ Руководство по развертыванию

### В процессе:
- 🔄 Интеграция frontend компонентов с API
- 🔄 Миграция данных из Local Storage

### Следующие шаги:
1. Заменить `useLocalStorage` на API вызовы в `Schedule.tsx`
2. Обновить CRUD операции во всех компонентах
3. Добавить обработку ошибок и loading states
4. Создать утилиту для миграции данных из Local Storage

## Deployment

Подробная информация по развертыванию доступна в файле `DEPLOYMENT.md`.

Основные варианты:
1. **Docker Compose** (рекомендуется) - простое развертывание всех сервисов
2. **Ручное развертывание** - для большего контроля

## Future Development

Планируемые модули (заглушки на главной странице):
- Дополнительные инструменты для управления задачами
- Другие утилиты для повышения продуктивности
- Система аутентификации пользователей

## Notes

- Проект ориентирован на использование 3-4 пользователями
- Данные хранятся в PostgreSQL на сервере
- Аутентификация пока не реализована (planned)
- Фокус на производительности и минимальном потреблении ресурсов
