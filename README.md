# RaboTA

Внутренний сайт для управления рабочими процессами и инструментов для личной продуктивности.

## Описание

RaboTA - это модульное веб-приложение для управления графиками работы сотрудников и другими бизнес-процессами. Проект построен на современном стеке технологий с фокусом на производительность и минимальное потребление ресурсов.

## Основные возможности

- 📅 **График работы** - Управление сменами и расписанием сотрудников
- 👥 **Управление сотрудниками** - Добавление, редактирование и удаление сотрудников
- ⏰ **Управление сменами** - Гибкая настройка рабочих смен
- 📊 **Визуальный календарь** - Удобный интерфейс для назначения смен

## Технологии

### Frontend
- React 19.2 + TypeScript
- Vite + Tailwind CSS
- React Router

### Backend
- Node.js 20 + Express
- TypeScript
- PostgreSQL 15

### DevOps
- Docker & Docker Compose
- Nginx

## Быстрый старт

### Вариант 1: Docker Compose (Рекомендуется)

```bash
# Клонировать репозиторий
git clone git@github.com:xyzmean/rabota_dev.git
cd rabota_dev

# Создать .env файл
cp .env.example .env
# Отредактируйте .env, установите DB_PASSWORD

# Запустить все сервисы
docker compose up -d

# Проверить статус
docker compose ps
```

Приложение будет доступно по адресу: `http://localhost`

### Вариант 2: Локальная разработка

#### Требования
- Node.js 20.x
- PostgreSQL 15
- npm или yarn

#### Backend

```bash
cd backend

# Установка зависимостей
npm install

# Настройка .env
cp .env.example .env
# Отредактируйте .env с вашими настройками PostgreSQL

# Запуск dev сервера
npm run dev
```

Backend будет доступен на `http://localhost:3001`

#### Frontend

```bash
cd work-schedule-app

# Установка зависимостей
npm install

# Настройка .env
cp .env.example .env

# Запуск dev сервера
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## Развертывание на сервере

Подробное руководство по развертыванию доступно в файле [DEPLOYMENT.md](./DEPLOYMENT.md).

### Системные требования

- CPU: 1 ядро
- RAM: 2GB
- Storage: 30GB
- OS: Ubuntu 20.04+ / Debian 10+

## Структура проекта

```
RaboTA/
├── backend/              # Backend API (Node.js + Express + PostgreSQL)
├── work-schedule-app/    # Frontend приложение (React + Vite)
├── docker-compose.yml    # Docker Compose конфигурация
├── DEPLOYMENT.md         # Руководство по развертыванию
├── CLAUDE.md             # Руководство для Claude Code
└── README.md             # Этот файл
```

## API Документация

### Employees API
- `GET /api/employees` - Список всех сотрудников
- `POST /api/employees` - Создать сотрудника
- `PUT /api/employees/:id` - Обновить сотрудника
- `DELETE /api/employees/:id` - Удалить сотрудника

### Shifts API
- `GET /api/shifts` - Список всех смен
- `POST /api/shifts` - Создать смену
- `PUT /api/shifts/:id` - Обновить смену
- `DELETE /api/shifts/:id` - Удалить смену

### Schedule API
- `GET /api/schedule?month=X&year=Y` - Получить график
- `POST /api/schedule` - Создать запись
- `POST /api/schedule/bulk` - Массовое создание

Полная документация API доступна в [CLAUDE.md](./CLAUDE.md).

## Разработка

### Команды

```bash
# Frontend
cd work-schedule-app
npm run dev        # Запуск dev сервера
npm run build      # Сборка для production
npm run lint       # Проверка кода

# Backend
cd backend
npm run dev        # Запуск dev сервера
npm run build      # Сборка для production
npm run migrate    # Запуск миграций БД

# Docker
docker compose up -d           # Запуск всех сервисов
docker compose logs -f         # Просмотр логов
docker compose down            # Остановка и удаление контейнеров
```

### Подключение к базе данных

```bash
# С Docker
docker compose exec postgres psql -U rabota_user rabota_db

# Без Docker
psql -U rabota_user -d rabota_db
```

## Мониторинг

### Health Check

```bash
# Backend
curl http://localhost:3001/health

# Результат: {"status":"OK","timestamp":"...","uptime":123}
```

### Логи

```bash
# Docker
docker compose logs backend
docker compose logs postgres

# PM2 (при ручном развертывании)
pm2 logs rabota-backend
```

## Резервное копирование

```bash
# Создание бэкапа
docker compose exec postgres pg_dump -U rabota_user rabota_db > backup_$(date +%Y%m%d).sql

# Восстановление из бэкапа
docker compose exec -T postgres psql -U rabota_user rabota_db < backup_20250115.sql
```

## Статус миграции

### Выполнено ✅
- Backend API сервер
- PostgreSQL схема и миграции
- REST API endpoints
- Docker Compose конфигурация
- API клиент для frontend
- Документация по развертыванию

### В процессе 🔄
- Интеграция frontend с API
- Миграция данных из Local Storage

## Поддержка

- **Issues**: [GitHub Issues](https://github.com/xyzmean/rabota_dev/issues)
- **Repository**: `git@github.com:xyzmean/rabota_dev.git`

## Лицензия

Проект для внутреннего использования.

---

**RaboTA** © 2025
